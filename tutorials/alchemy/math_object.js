
// The function Math.random generates a function between 0 and 1,
// not including 1.

// Random Number betwen 0 and 100
const r1= Math.random() * 100;
console.log(r1);

// randomNumber will be between 25 and 100
const randomNumber = (Math.random() * 75) + 25;
console.log(`${randomNumber} \n`);

// Rounding a number to the nearest integer
console.log(`${Math.floor(2.3456)}`);